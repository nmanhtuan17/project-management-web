export enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system"
}

export enum LayoutType {
  GRID = "grid",
  LIST = "list",
}

export enum FormAction {
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
  VIEW = "view",
}


export enum Role {
  ADMIN = "admin",
  MANAGER = "manager",
  USER = "user",
}